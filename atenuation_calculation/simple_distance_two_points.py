import numpy as np
import math

a = np.array([1,2,6,78,9])
b = np.array([12,2,46,8,9])

def distancia(p1, p2): #p1 y p2 son dos vectores (x,y)
    resto = p2**2-p1**2
    dist = math.sqrt(abs(resto[0])+ abs(resto[1]))
    return dist


def interseccion():

    for i to [1,2,3,4]:
        if != 0:
            print

    return

def recta():


    return


distancia(a,b)
